# ═══════════════════════════════════════════════════
#  Aura v14.0 — Requirements (Full Bundle)
#  Install: pip install -r requirements.txt
#  Then:    playwright install chromium
# ═══════════════════════════════════════════════════

# ── Core HTTP & Networking ──────────────────────────
aiohttp>=3.13.0
aiofiles>=25.0.0
requests>=2.32.0
requests-toolbelt>=1.0.0
httpx>=0.28.0
urllib3>=2.6.0
certifi>=2026.1.0

# ── Web Scraping & Parsing ──────────────────────────
beautifulsoup4>=4.14.0
lxml>=5.0.0

# ── Browser Automation (Playwright) ────────────────
playwright>=1.58.0
# After install run: playwright install chromium

# ── DNS & Network Tools ─────────────────────────────
dnspython>=2.8.0
PySocks>=1.7.1
scapy>=2.7.0
python-nmap>=0.7.1

# ── AI / Gemini ─────────────────────────────────────
google-genai>=1.65.0
google-generativeai>=0.8.0

# ── CLI & Display ───────────────────────────────────
rich>=14.0.0
rich-click>=1.9.0
click>=8.3.0
colorama>=0.4.6
tqdm>=4.67.0

# ── Web Frameworks (API / Dashboard) ────────────────
Flask>=3.1.0
Flask-Compress>=1.23
flask-cors>=6.0.0
flask-socketio>=5.6.0
flask-talisman>=1.1.0
fastapi>=0.133.0
uvicorn>=0.41.0
starlette>=0.52.0
simple-websocket>=1.1.0
websockets>=16.0

# ── Data Validation & Serialization ─────────────────
pydantic>=2.12.0
PyYAML>=6.0.0
tomli>=2.4.0
tomli_w>=1.2.0
Jinja2>=3.1.0

# ── Security & Crypto ───────────────────────────────
cryptography>=46.0.0
PyJWT>=2.11.0

# ── PDF Report Generation ───────────────────────────
reportlab>=4.4.0

# ── Image Processing ────────────────────────────────
Pillow>=12.0.0

# ── cURL Impersonation (WAF bypass) ─────────────────
curl_cffi>=0.14.0

# ── Environment & Config ────────────────────────────
python-dotenv>=1.2.0

# ── OSINT API Integrations ──────────────────────────
shodan>=1.31.0
